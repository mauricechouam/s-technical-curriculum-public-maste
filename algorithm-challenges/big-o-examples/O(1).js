function printFirstItem(arr) {
    console.log(arr[0]);
}

/*
This function will run in O(1) or “constant time” because no matter 
whether there is 1 item or 1000 items, this will only execute one step.
*/
