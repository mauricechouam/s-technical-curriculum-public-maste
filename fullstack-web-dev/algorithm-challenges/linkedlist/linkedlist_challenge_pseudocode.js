
    insertAt(element, index) {
        //*************************/
        //**** YOUR CODE HERE *****/
        //*************************/

        // check whether the index is greater than 0  and if it is greater than the current size of the linked list
        // if so, return false and exit

        // otherwise, we create a new node with the value passed in
        // determine where the new node is going to be inserted

        // if the index is 0, then the node becomes the head
        // if the index is not 0, then iterate over the list using a while loop to insert it at the
        // respective position
        
        // adding an element

        // increase the size of the list
        //*************************/
    }
